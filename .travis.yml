language: python
sudo: false
services:
  - docker
python:
  - "3.5.1"
  - "3.5"
  - "3.6"
install:
  - pip install tox tox-travis codecov
script:
  - tox
after_success:
  - codecov
cache:
  directories:
    - $HOME/.cache/pip
notifications:
  email: false
